# Written by `npx convex dev` — do not fill these in by hand.
# CONVEX_DEPLOYMENT=
# VITE_CONVEX_URL=
# VITE_CONVEX_SITE_URL=

# Set on the Convex deployment, NOT here:
#   npx convex env set ADMIN_EMAIL vous@example.com
# Whoever signs up with that address becomes the first active admin.
